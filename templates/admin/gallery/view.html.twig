{% extends 'admin/dashboard.html.twig' %}

{% block title 'Vue Generale' %}
{% block body %}


<!-- Page Content -->
<div class="container">

  <h1 class="font-weight-light text-center text-lg-left mt-4 mb-0">Album photos</h1>

  <hr class="mt-2 mb-5">

  <div class="row text-center text-lg-left">
            {% for gallery in gallery %}

    <div class="col-lg-3 col-md-4 col-6">
      <a href="{{ vich_uploader_asset(gallery,'photo')  }}" class="d-block mb-4 h-100">
            <img class="img-fluid img-thumbnail" src="{{ vich_uploader_asset(gallery,'photo') }}"style="width:400px ; height: 300px" alt="">
          </a>
    </div>
                {% endfor %}

  </div>

</div>
<!-- /.container -->


{% endblock body %}