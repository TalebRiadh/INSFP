{% extends 'admin/dashboard.html.twig' %}

{% block body %}

<div class="container mt-4">
    <h2 style="margin-bottom:80px;">
        <i class="fas fa-newspaper fa-2x" style="padding-right:5px;"></i>  <a href="{{ path('specialite')}}">Option</a>\Editer Les Modules 
    </h2>           
    <span id="nomspecialite" class="id_spc" style="display:none;">{{ specialite.name}}</span>
      {% for module in modules %}
                <span id="module" class="bdg badge badge-secondary" style="display:none" >{{module.nom}}</span>
                            {% endfor %}     
 {{ form_start(form_m) }}
                        {{ form_row(form_m.nom) }}
                {{ form_end(form_m)}}
</div>
{% endblock %}